const sentence = "JavaScript is fun";

// Split the sentence into words, reverse the order of words, and join them back into a string
const reversedWords = sentence.split(" ").reverse().join(" ");

console.log(reversedWords); // "fun is JavaScript"

const myReverse = sentence.split(" ").reverse().join(" ");
console.log(myReverse);
